#ifndef UTILS_H
#define UTILS_H

// Declara a função para iniciar um delay com a duração especificada (em milissegundos)
void iniciarDelay(unsigned long duracao);

// Declara a função para gerenciar o delay ativo
void gerenciarDelay();

#endif